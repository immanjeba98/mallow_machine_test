// import { toast } from '';
// export const Toast = ({ type = 'success', message = '', title = '', loading = true }: any) => {
//     if (!!message) {
//         if (type === 'success') {
//             toast.success(message);
//         } else if (type === 'error' || type === 'danger') {
//             toast.error(message, { duration: 2000 });
//         } else {
//             // Handle the case where 'type' doesn't match any known method
//             console.error(`Unknown toast type: ${type}`);
//         }
//     }
// }